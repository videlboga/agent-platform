name: E2E Visual Regression

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Restore Playwright browsers cache
        id: playwright-cache-restore
        uses: actions/cache/restore@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('.github/workflows/e2e.yml') }}

      - name: Install pip dependencies
        run: |
          python3 -m pip install --quiet --upgrade pip
          pip install --quiet playwright pillow requests pixelmatch

      - name: Install Playwright Chromium
        if: steps.playwright-cache-restore.outputs.cache-hit != 'true'
        run: |
          python3 -m playwright install chromium

      - name: Start test server & take screenshots
        run: |
          python3 -m http.server 8080 --directory e2e-test &
          SERVER_PID=$!
          sleep 2
          curl -sf http://localhost:8080/ > /dev/null && echo "Server is up"

          python3 scripts/e2e-screenshot.py \
            --url http://localhost:8080/ \
            --output screenshots/current/dashboard.png \
            --diff-output screenshots/current/dashboard.diff.png \
            --baseline screenshots/baseline/dashboard.png \
            --viewport 1280,720 \
            --wait 2 \
            --selector "body" \
            --threshold 0.05

          kill $SERVER_PID 2>/dev/null || true

      - name: Save Playwright browsers cache
        uses: actions/cache/save@v4
        if: always() && steps.playwright-cache-restore.outputs.cache-hit != 'true'
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('.github/workflows/e2e.yml') }}

      - name: Upload screenshots on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-diffs
          path: |
            screenshots/current/
            screenshots/baseline/
          retention-days: 14

      - name: Upload screenshots always
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-screenshots
          path: screenshots/current/
          retention-days: 7